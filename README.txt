Task done:
PHP program to take google authentication to redirect the webapp to given menu json link.

Files:
config - Needed for google project API(for authentication)
Index - Starting program consists of button to authenticate with google and show profile.
Menu - a php file to redirect the index page to menu page.

Screen shots of execution are provided in wordfile.
These files were directly placed in C:\xampp\htdocs and not in a specific project.
Server used is XAMPP(Apache).

First file to run is Idex.php.

System used: i7 win 10 64bit , Google chrome , Microsoft edge.